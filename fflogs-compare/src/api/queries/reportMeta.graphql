query ReportMeta($code: String!, $fightIDs: [Int!]!) {
  reportData {
    report(code: $code) {
      title
      startTime
      endTime
      fights(fightIDs: $fightIDs) {
        id
        name
        kill
        startTime
        endTime
        lastPhaseAsAbsoluteIndex
      }
    }
  }
}
